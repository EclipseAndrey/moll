import 'package:flutter/material.dart';

class ViewHost extends StatelessWidget {
  const ViewHost({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
