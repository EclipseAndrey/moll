import 'package:flutter/material.dart';

class ErrorHost extends StatelessWidget {
  const ErrorHost({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
